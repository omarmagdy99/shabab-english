# خطة عمل وتصميم تطبيق إدارة طلاب دورات الإنجليزية (Shabab English)

> **الإصدار 3** — محدّث بعد قرارات صاحب المشروع: فصل الحالة الجامعية عن بيانات المعهد، وإضافة الأرشفة وسجل العمليات مع التراجع عن أي عملية.

---

## 1. نظرة عامة

تطبيق ويب بسيط لإدارة وتسجيل طلاب دورة اللغة الإنجليزية، يعمل بالكامل بدون سيرفرات أو قواعد بيانات تقليدية.

- **المستخدمون:** طاقم الإدارة والإشراف (1-10 مشرفين). الطلاب لا يستخدمونه.
- **الاستخدام:** أكثر من 90% من الهاتف المحمول ← واجهة عربية RTL مصممة للموبايل أولاً.
- **الاستضافة:** GitHub Pages مجاناً.
- **قاعدة البيانات:** Google Sheet عبر Google Apps Script.
- **الشاشات الأربع:** الطلاب / المعلمون / مولّد الرسائل / سجل العمليات.

---

## 2. القرارات الأساسية

| القرار | السبب | البديل المرفوض |
| :--- | :--- | :--- |
| **رقم سري بسيط للدخول (`55555`) بدون نظام حسابات** | صاحب المشروع قرر أن الحماية ليست مطلبًا هنا؛ الأولوية لسهولة الدخول من الموبايل. | رفض نظام مستخدمين وصلاحيات — تعقيد بلا مقابل لفريق من 10 أشخاص. |
| **رابط الـ Web App مكتوب داخل `js/config.js`** | المشرف يفتح الموقع ويكتب `55555` فقط — لا شيء آخر يُلصق. | رفض مطالبة كل مشرف بلصق رابط طويل في أول تشغيل. |
| **Apps Script Web App عبر POST حصراً** | نقطة اتصال واحدة موحّدة، وتفادي CORS Preflight. | رفض **Published CSV** لأنه ينشر الشيت للعامة بلا رجعة. |
| **إلغاء ورقة `config`** | المستويات والسنوات ثابتة في الكود، وأسماء المعلمين تأتي من ورقة `teachers`. | رفض ورقة إعدادات يدوية يصعب شرحها وتعبئتها. |
| **ورقة `teachers` مستقلة + مزامنة تلقائية** | الطالب الذي مستواه `معلم` يظهر تلقائياً في المعلمين، مع إمكانية إضافة معلم يدوياً. | رفض تكرار كتابة أسماء المعلمين في مكانين. |
| **فصل `uni_status` عن `enrolled` تماماً** | "طالب/خريج" تخص الجامعة، و"مسجل بالمعهد" والسنة تخص المعهد — لا علاقة بينهما إطلاقاً. | رفض ربط ظهور السنة بالحالة الجامعية (كان خطأ في الإصدار 2). |
| **سجل عمليات كامل + تراجع (Undo) عن أي عملية** | خطأ واحد على الموبايل قد يمسح عشرات السجلات؛ لا بد من طريق رجوع. | رفض الاعتماد على Version history في Google Sheets — غير عملي من الموبايل ولا يفهمه غير التقني. |
| **الأرشفة بدل الحذف النهائي** | كل محذوف ينتقل لورقة `archive` بدل أن يختفي، فيمكن استرجاعه ومراجعته. | رفض `deleteRow` المباشر الذي لا رجعة فيه. |
| **حفظ `mentor` كاسم نصي + إعادة تسمية متتالية** | الشيت يظل مقروءاً بالعين، والاستيراد يعمل بالأسماء مباشرة. | رفض تخزين `teacher_id` مشفّر يجعل الشيت غير مفهوم. |
| **Vanilla JS (ES Modules) بدون Framework أو Build Step** | النشر = `git push` فقط. | رفض React / Vue / Vite. |
| **محرك استعلام موحد (Shared Query Engine)** | تطابق نتيجة الفلترة والترتيب بين الجدول ومولّد الرسائل. | رفض كود فلترة منفصل لكل شاشة. |
| **LockService + OCC (`updated_at`)** | منع تضارب تعديلات المشرفين. | رفض Last-Write-Wins الجاهل. |
| **واجهة هاتف مخصصة (Sticky Column + Bottom Sheets)** | استغلال شاشة 375px. | رفض القوائم المنسدلة الضيقة. |
| **تطبيع النصوص العربية** | البحث عن "احمد" يجب أن يجد "أحمد". | رفض المقارنة الحرفية المباشرة. |

---

## 3. المعمارية (Architecture)

```text
+-------------------------------------------------------------------+
|                     هاتف المشرف (Mobile Browser)                   |
|  - static HTML / CSS / Vanilla JS (ES Modules)                    |
|  - localStorage (Cache + Passcode + Query State + Templates)      |
+-------------------------------------------------------------------+
                                 |
                                 |  تحميل الملفات الثابتة (HTTPS)
                                 v
+-------------------------------------------------------------------+
|                   GitHub Pages (Static Hosting)                   |
|  - يقدم الملفات مباشرة من المستودع                                 |
|  - بدون build step / بدون منطق سيرفر / بدون بيانات طلاب            |
+-------------------------------------------------------------------+
                                 |
                                 |  HTTP POST - text/plain JSON
                                 |  { action, pass, ...data }
                                 v
+-------------------------------------------------------------------+
|                  Google Apps Script Web App (API)                 |
|  - التحقق من الرقم السري                                           |
|  - LockService (انتظار حتى 10 ثوانٍ)                               |
|  - فحص OCC عبر updated_at                                         |
|  - مزامنة المعلمين تلقائياً                                        |
|  - تسجيل كل عملية في السجل + الأرشفة عند الحذف                     |
+-------------------------------------------------------------------+
                                 |
                                 |  SpreadsheetApp Read / Write
                                 v
+-------------------------------------------------------------------+
|                   Google Sheet ("Database")                       |
|  Tab: students | teachers | archive | log                         |
+-------------------------------------------------------------------+
```

---

## 4. نموذج البيانات (Data Model)

### أولاً: ورقة الطلاب (`students`)

| اسم العمود | الاسم بالعربي | النوع | مطلوب؟ | القواعد |
| :--- | :--- | :--- | :---: | :--- |
| `id` | المعرف الفريد | نص | نعم | ينشأ تلقائياً (Timestamp + Random). |
| `name` | الاسم | نص | نعم | حرفان على الأقل. |
| `phone` | رقم الهاتف | نص | نعم | يطابق `^01[0125][0-9]{8}$`. **يُحفظ كنص** والعمود منسّق Plain text. |
| `level` | المستوى | نص | نعم | `معلم` ، `أ` ، `أ/` ، `ب` ، `ب/` ، `ج` ، `ج/` — ثابتة في الكود. |
| `mentor` | المعلم المتابع | نص | **لا** | اختياري. إذا كُتب فيجب أن يطابق اسم معلم موجود في ورقة `teachers`. الطالب بلا معلم يُجمَّع تحت "أخرى / غير محدد" في الرسائل. |
| `uni_status` | الحالة الجامعية | نص | نعم | `طالب` أو `خريج` — **تخص الجامعة فقط**. |
| `enrolled` | مسجل بالمعهد؟ | نص | نعم | `نعم` أو `لا` — **مستقل تماماً عن الحالة الجامعية**. |
| `year` | السنة بالمعهد | نص | مشروط | `تمهيدي` ، `الأولى` ، `الثانية` ، `الثالثة` ، `الرابعة`. مطلوب **فقط** إذا `enrolled = نعم`، بغض النظر عن الحالة الجامعية. |
| `notes` | ملاحظات | نص | لا | نص حر. |
| `created_at` | تاريخ الإضافة | نص | نعم | ISO 8601 UTC. |
| `updated_at` | تاريخ التحديث | نص | نعم | يُستخدم في منع التضارب وفي فحص صلاحية التراجع. |

> **الفصل بين الحقلين مقصود وصريح:** `uni_status` تصف وضع الشخص في **جامعته**، و `enrolled` و `year` تصفان وضعه في **المعهد**. لا يؤثر أحدهما في الآخر إطلاقاً. مثال صحيح تماماً: `uni_status = خريج` و `enrolled = نعم` و `year = تمهيدي`.

### ثانياً: ورقة المعلمين (`teachers`)

| اسم العمود | الاسم بالعربي | النوع | مطلوب؟ | القواعد |
| :--- | :--- | :--- | :---: | :--- |
| `id` | المعرف الفريد | نص | نعم | ينشأ تلقائياً. |
| `name` | الاسم | نص | نعم | فريد — لا يُسمح بمعلمَين بنفس الاسم. |
| `phone` | رقم الهاتف | نص | نعم | نفس قاعدة هاتف الطالب. |
| `source` | مصدر التسجيل | نص | نعم | `تلقائي` (من طالب مستواه معلم) أو `يدوي`. |
| `student_id` | معرّف الطالب المرتبط | نص | مشروط | يُملأ فقط عندما `source = تلقائي`. |
| `notes` | ملاحظات | نص | لا | نص حر. |
| `created_at` / `updated_at` | تواريخ | نص | نعم | ISO 8601 UTC. |

### ثالثاً: ورقة الأرشيف (`archive`)

كل سجل محذوف ينتقل هنا بدل أن يختفي — مقروءة بالعين عند فتح الشيت:

أعمدتها هي **اتحاد أعمدة كل الكيانات القابلة للأرشفة** (طالب + معلم + قالب)، فتستوعبها جميعاً
(الخانات غير المعنية تُترك فارغة):

```text
entity | id | name | phone | level | mentor | uni_status | enrolled | year |
notes | source | student_id | created_at | updated_at | deleted_at | log_id |
content | query_json
```

> ⚠️ **قاعدة حرجة:** أي حقل موجود في كيان وغير موجود في `ARCHIVE_FIELDS` **يُفقد نهائياً عند الحذف**
> ولا يعود بالاسترجاع ولا بالتراجع. عند إضافة حقل جديد لأي كيان، أضفه هنا أيضاً — **في آخر القائمة فقط**،
> لأن الإدراج في المنتصف يزيح كل بيانات الأرشيف القديمة. يوجد حارس في `archiveRecords_` يسجّل تحذيراً
> في Logger عند اكتشاف حقل غير مغطّى.

| العمود الإضافي | الوصف |
| :--- | :--- |
| `entity` | `طالب` أو `معلم` أو `قالب` |
| `deleted_at` | وقت الحذف |
| `log_id` | معرّف العملية في ورقة `log` — يربط المحذوف بعملية التراجع الخاصة به |
| `content` / `query_json` | خاصان بالقوالب — نص القالب والفلتر المحفوظ معه |

### رابعاً: ورقة القوالب (`templates`)

قوالب الرسائل المحفوظة، مشتركة بين كل الأجهزة (بدل حفظها محلياً على هاتف واحد):

| اسم العمود | الوصف |
| :--- | :--- |
| `id` | معرّف القالب |
| `name` | اسم القالب كما يظهر في قائمة شاشة الرسائل |
| `content` | نص القالب بمتغيراته |
| `query_json` | الفلتر والبحث والترتيب المحفوظ مع القالب (JSON) — يُطبَّق تلقائياً عند اختياره |
| `created_at` / `updated_at` | تواريخ ISO 8601 UTC |

### خامساً: ورقة سجل العمليات (`log`)

قلب ميزة التراجع. **كل** عملية كتابة تُسجَّل هنا داخل نفس القفل:

| اسم العمود | الوصف |
| :--- | :--- |
| `id` | معرّف العملية |
| `timestamp` | وقت التنفيذ (ISO 8601 UTC) |
| `action` | `إضافة` / `تعديل` / `حذف` / `تعديل جماعي` / `حذف جماعي` / `استيراد` / `تراجع` |
| `entity` | `طالب` أو `معلم` |
| `summary_ar` | وصف عربي جاهز للعرض، مثال: `تعديل: أحمد محمود — المستوى من (ب) إلى (أ)` |
| `target_ids` | معرّفات السجلات المتأثرة، مفصولة بفاصلة |
| `before_json` | الحالة الكاملة قبل العملية (JSON) — **مصدر التراجع** |
| `after_json` | الحالة الكاملة بعد العملية (JSON) — لفحص هل تغيّر السجل بعدها |
| `side_effects_json` | الآثار الجانبية (مثل: معلم أُنشئ تلقائياً، أو إعادة تسمية متتالية) |
| `undone` | `نعم` / `لا` |
| `undone_at` | وقت التراجع إن حدث |
| `undo_of` | إذا كان هذا السطر نفسه عملية تراجع، يحمل معرّف العملية الأصلية |

> **تنظيف تلقائي:** يُحذف من السجل ما تجاوز **90 يوماً** لمنع تضخم الشيت. الأرشيف لا يُنظَّف.

### سادساً: القوائم الثابتة (`js/constants.js`)

```js
export const LEVELS     = ['معلم', 'أ', 'أ/', 'ب', 'ب/', 'ج', 'ج/'];
export const YEARS      = ['تمهيدي', 'الأولى', 'الثانية', 'الثالثة', 'الرابعة'];
export const UNI_STATUS = ['طالب', 'خريج'];   // الحالة في الجامعة
// قائمة المعلمين ليست ثابتة — تُقرأ من ورقة teachers في كل تحميل
```

### سابعاً: ترتيب المستويات

| قيمة المستوى | الرتبة (`sort_rank`) |
| :--- | :---: |
| `معلم` | 0 (الأعلى) |
| `أ` | 1 |
| `أ/` | 2 |
| `ب` | 3 |
| `ب/` | 4 |
| `ج` | 5 |
| `ج/` | 6 (الأقل) |
| فارغ أو غير معروف | 99 (آخر القائمة دائماً) |

### ثامناً: توضيح اللبس في كلمة "معلم"

- **المستوى (`level`):** القيمة `"معلم"` هي **درجة كفاءة** عليا للطالب.
- **المعلم المتابع (`mentor`):** الشخص الذي يتابع الطالب، اسمه من ورقة `teachers`.
- الطالب الذي وصل لمستوى `معلم` يصبح **مؤهلاً** لمتابعة طلاب آخرين، ولهذا يُضاف تلقائياً لورقة `teachers` — انظر القسم 7.

---

## 5. طبقة الـ API (Apps Script Contract)

كل الطلبات **HTTP POST** بحمولة `text/plain` تحتوي JSON مُسلسَل، لتفادي CORS Preflight.

```js
// api.js
await fetch(WEB_APP_URL, {
  method: 'POST',
  headers: { 'Content-Type': 'text/plain;charset=utf-8' },
  body: JSON.stringify({ action, pass, ...payload })
});
```

> **`doGet` غير مستخدم** — يرد برسالة ثابتة `"not used"` بدون لمس البيانات.

### الاستجابة الموحدة

- **نجاح:** `{"status": "success", "data": ...}`
- **خطأ:** `{"status": "error", "code": <رقم>, "message": "وصف بالعربي"}`

| الرمز | المعنى |
| :---: | :--- |
| `401` | الرقم السري غير صحيح |
| `400` | بيانات ناقصة أو مخالفة للتحقق |
| `404` | السجل غير موجود |
| `409` | تضارب: عُدّل السجل من مشرف آخر |
| `423` | فشل القفل خلال 10 ثوانٍ — أعد المحاولة |
| `428` | ممنوع: المعلم متابع لطلاب (انظر 7.د) |
| `410` | التراجع غير ممكن (انظر القسم 9) |

### قائمة العمليات

| `action` | الوظيفة |
| :--- | :--- |
| `list` | جلب الطلاب والمعلمين وآخر 200 عملية دفعة واحدة |
| `create` / `update` / `delete` | طالب واحد |
| `bulkCreate` | استيراد دفعة من الإكسيل |
| `bulkUpdate` | تعديل حقل واحد على عدة طلاب مختارين |
| `bulkDelete` | حذف عدة طلاب مختارين |
| `createTeacher` / `updateTeacher` / `deleteTeacher` | معلم واحد |
| `undo` | التراجع عن عملية من السجل |
| `listArchive` | عرض المؤرشفين (يُطلب عند فتح شاشة الأرشيف فقط) |
| `restore` | استرجاع سجل من الأرشيف مباشرة |

**كل عملية كتابة تُنفَّذ داخل نفس القفل بالترتيب:** التحقق من الرقم السري ← فحص OCC ← تنفيذ التغيير ← الأرشفة إن كان حذفاً ← مزامنة المعلمين ← كتابة سطر السجل. إما أن تتم كلها أو لا شيء.

---

### 1. `list`

**request:** `{ "action": "list", "pass": "55555" }`

**response:**
```json
{
  "status": "success",
  "data": {
    "students": [
      {
        "id": "std_101",
        "name": "أحمد محمود",
        "phone": "01012345678",
        "level": "أ",
        "mentor": "حسام عادل",
        "uni_status": "خريج",
        "enrolled": "نعم",
        "year": "تمهيدي",
        "notes": "",
        "created_at": "2026-07-25T10:00:00Z",
        "updated_at": "2026-07-25T10:00:00Z"
      }
    ],
    "teachers": [
      { "id": "tch_01", "name": "حسام عادل", "phone": "01099998888",
        "source": "يدوي", "student_id": "", "notes": "",
        "created_at": "2026-07-20T09:00:00Z", "updated_at": "2026-07-20T09:00:00Z" }
    ],
    "log": [
      { "id": "log_882", "timestamp": "2026-07-25T10:00:00Z", "action": "تعديل",
        "entity": "طالب", "summary_ar": "تعديل: أحمد محمود — المستوى من (ب) إلى (أ)",
        "target_ids": "std_101", "undone": "لا", "can_undo": true }
    ]
  }
}
```

> حقول `before_json` و `after_json` **لا تُرسل** في `list` (لتخفيف الحمل) — تُقرأ داخل السيرفر وقت التراجع فقط. الحقل `can_undo` محسوب في السيرفر ويخبر الواجهة هل يُعرض زر التراجع مفعّلاً أم رمادياً.

### 2. `create` / `update` / `delete` (طالب)

```json
{ "action": "create", "pass": "55555",
  "student": { "name": "سارة علي", "phone": "01198765432", "level": "ب",
               "mentor": "حسام عادل", "uni_status": "طالب",
               "enrolled": "لا", "year": "", "notes": "" } }
```

```json
{ "action": "update", "pass": "55555",
  "student": { "id": "std_101", "...": "...",
               "expected_updated_at": "2026-07-25T10:00:00Z" } }
```

```json
{ "action": "delete", "pass": "55555", "id": "std_101",
  "expected_updated_at": "2026-07-25T10:00:00Z" }
```

عند `level = "معلم"` تحتوي الاستجابة أيضاً على المعلم المُنشأ تلقائياً، وكل استجابة ناجحة تحتوي `log_id` للعملية.

### 3. `bulkUpdate` / `bulkDelete`

```json
{ "action": "bulkUpdate", "pass": "55555",
  "ids": ["std_101", "std_102", "std_140"],
  "field": "mentor", "value": "مريم سمير" }
```

```json
{ "action": "bulkDelete", "pass": "55555", "ids": ["std_101", "std_102"] }
```

> `field` مسموح فقط ضمن: `level`, `mentor`, `uni_status`, `enrolled`, `year`. الاسم والهاتف لا يُعدَّلان جماعياً.

### 4. `undo`

```json
{ "action": "undo", "pass": "55555", "log_id": "log_882" }
```

**response (نجاح):**
```json
{ "status": "success",
  "data": { "restored_count": 12, "skipped": [],
            "message": "تم التراجع عن: حذف جماعي — 12 طالب" } }
```

**response (تراجع جزئي):**
```json
{ "status": "success",
  "data": { "restored_count": 10,
            "skipped": [ { "id": "std_140", "reason": "تم تعديل هذا الطالب بعد العملية" },
                         { "id": "std_141", "reason": "رقم الهاتف مسجل الآن لطالب آخر" } ],
            "message": "تم التراجع عن 10 من 12 — راجع المستثنى" } }
```

**response (رفض كامل):**
```json
{ "status": "error", "code": 410,
  "message": "لا يمكن التراجع: تم التراجع عن هذه العملية من قبل." }
```

### 5. عمليات المعلمين

```json
{ "action": "createTeacher", "pass": "55555",
  "teacher": { "name": "مريم سمير", "phone": "01555556666", "notes": "" } }
```

**response عند المنع:**
```json
{ "status": "error", "code": 428,
  "message": "لا يمكن حذف هذا المعلم لأنه متابع لـ 7 طلاب. انقل الطلاب لمعلم آخر أولاً.",
  "data": { "students_count": 7, "student_names": ["أحمد محمود", "سارة علي"] } }
```

---

## 6. الشاشة الأولى — التسجيل وجدول الطلاب

### أ. نموذج التسجيل (Bottom Sheet)

| الحقل | النوع | قاعدة التحقق |
| :--- | :--- | :--- |
| الاسم | نص | مطلوب، حرفان على الأقل |
| رقم الهاتف | نص (لوحة أرقام) | مطلوب، يطابق `^01[0125][0-9]{8}$` بعد التطبيع |
| المستوى | قائمة منسدلة | مطلوب، من السبعة حصراً |
| المعلم المتابع | قائمة منسدلة | مطلوب، من ورقة `teachers` |
| **الحالة الجامعية** | مفتاح تحويل [طالب / خريج] | مطلوب — **لا يؤثر على أي حقل آخر** |
| **مسجل بالمعهد؟** | مفتاح تحويل [نعم / لا] | مطلوب |
| **السنة بالمعهد** | قائمة منسدلة | **تظهر وتصبح مطلوبة فقط إذا `مسجل بالمعهد = نعم`** — بغض النظر عن الحالة الجامعية |
| ملاحظات | نص حر | اختياري |

يظهر الحقلان في مجموعتين بصريتين منفصلتين داخل النموذج حتى لا يختلطا:

```text
┌─ بيانات الجامعة ──────────────┐
│  الحالة:  ( طالب ) [ خريج ]   │
└───────────────────────────────┘
┌─ بيانات المعهد ───────────────┐
│  مسجل بالمعهد؟  [ نعم ] ( لا ) │
│  السنة: ▾ تمهيدي              │
└───────────────────────────────┘
```

**عند اختيار المستوى `معلم`** يظهر تنبيه أزرق:
> *"سيتم تسجيل هذا الطالب تلقائياً في قائمة المعلمين، وسيصبح متاحاً كمتابع لطلاب آخرين."*

**التحقق من التكرار:** عند كتابة رقم الهاتف يُفحص فوراً مقابل الأرقام المحمّلة (بعد التطبيع). لو مكرر: *"هذا الرقم مسجل بالفعل للطالب: [الاسم]"* مع زر لاستعراضه. المنع نهائي.

### ب. جدول الطلاب

- **العمود الأول (الاسم) مثبّت أفقياً** وباقي الأعمدة تُمرَّر أفقياً.
- **الأعمدة الافتراضية:** الاسم، رقم الهاتف، المستوى، الحالة الجامعية، مسجل بالمعهد، السنة، المعلم المتابع.
- **إجراءات الصف:** ✏️ تعديل ، 🗑️ حذف ، 💬 واتساب (`https://wa.me/2<phone>`).
- **عدّاد أعلى الجدول:** *"عرض 42 من 137 طالب"*.

### ج. التحديد والعمليات الجماعية

- **Checkbox** في بداية كل صف، و**"تحديد الكل"** في رأس الجدول يحدد **الصفوف الظاهرة بعد الفلترة فقط**:
  > *"تم تحديد 42 طالب (نتيجة الفلترة الحالية) — [تحديد كل الـ 137 طالب بدلاً من ذلك]"*
- بمجرد تحديد صف يظهر شريط إجراءات سفلي ثابت:

```text
+---------------------------------------------------+
|  ✅ 42 محدد    [✏️ تعديل جماعي]  [🗑️ حذف]  [✖ إلغاء] |
+---------------------------------------------------+
```

- **التعديل الجماعي:** درج سفلي `[الحقل ▾] [القيمة الجديدة ▾]` ← يطبّق على كل المحددين. الحقول: المستوى، المعلم المتابع، الحالة الجامعية، مسجل بالمعهد، السنة.
- **الحذف الجماعي:** تأكيد صريح بكتابة العدد:
  > *"سيتم نقل 42 طالب إلى الأرشيف. اكتب الرقم 42 للتأكيد."*
  ثم يظهر شريط تراجع فوري لمدة 10 ثوانٍ: **↩️ تراجع** — والعملية تظل قابلة للتراجع من شاشة السجل بعد ذلك.

### د. عناصر التحكم الثلاثة

```text
+-----------------------------------+
|  [🔍 البحث بالاسم أو الهاتف...]    |
|  [⚡ تصفية (2)]   [👁️ الأعمدة (4)] |
|  [↕️ الترتيب: المستوى ▲]           |
+-----------------------------------+
| ☐ | الاسم       | المستوى | الهاتف |
| ---------------------------------- |
| ☐ | أحمد محمد   | أ      | 010... |
| ☑ | سارة محمود  | ب/     | 011... |
+-----------------------------------+
```

1. **محدد الأعمدة:** درج سفلي بقائمة كل الأعمدة، لكل عمود مفتاح إظهار/إخفاء + سهمان لأعلى/لأسفل (أزرار لمس بدل السحب والإفلات). الاختيار يُحفظ ويعود كما هو عند إعادة الفتح.

2. **مُصفّي البيانات:** درج سفلي بشروط قابلة للإضافة والحذف. الفلترة على **أي حقل حتى لو كان عموده مخفياً**. الشرط: `[الحقل ▾] [نوع المقارنة ▾] [القيمة]`. أزرار: **تطبيق** و **إعادة ضبط**.

   | المُعامل | الوصف |
   | :--- | :--- |
   | `contains` | يحتوي على |
   | `equals` | يساوي تماماً |
   | `notEquals` | لا يساوي |
   | `startsWith` | يبدأ بـ |
   | `in` | ضمن قائمة اختيارات |
   | `isEmpty` / `notEmpty` | فارغ / غير فارغ |

3. **مُرتِّب البيانات:** حسب أي حقل (حتى المخفي)، تصاعدي أو تنازلي، ومتعدد المستويات. المستوى يُرتَّب رتبياً (معلم ← ج/) وليس أبجدياً.

### هـ. تطبيع النصوص العربية — `normalize.js`

**كل مقارنة نصية في الفلترة والبحث وكشف التكرار تمر عليها أولاً** (البيانات الأصلية تبقى كما هي في الشيت):

| القاعدة | التحويل | مثال |
| :--- | :--- | :--- |
| توحيد الألف | `أ` `إ` `آ` `ٱ` ← `ا` | أحمد = احمد |
| توحيد التاء المربوطة | `ة` ← `ه` | فاطمة = فاطمه |
| توحيد الياء | `ى` ← `ي` | يحيى = يحيي |
| توحيد الهمزة | `ؤ` ← `و` ، `ئ` ← `ي` | مؤمن = مومن |
| إزالة التشكيل | حذف `ً-ْ` والتطويل `ـ` | مُحَمَّد = محمد |
| الأرقام الهندية | `٠-٩` و `۰-۹` ← `0-9` | ٠١٠١٢ = 01012 |
| المسافات | دمج المتكرر + إزالة الأطراف | "أحمد  علي " = "احمد علي" |
| الهاتف | حذف المسافات و `-` و `+` و `()`، وتحويل `+20`/`0020`/`20` ← `0` | ‎+20 101 234 5678 = 01012345678 |

---

## 7. الشاشة الثانية — المعلمون

مصدر قائمة "المعلم المتابع" في كل مكان في التطبيق.

### أ. الجدول

| العمود | ملاحظات |
| :--- | :--- |
| الاسم | — |
| رقم الهاتف | مع زر واتساب 💬 |
| المصدر | شارة: 🟢 `تلقائي` أو 🔵 `يدوي` |
| عدد الطلاب المتابعين | محسوب لحظياً، وقابل للنقر ← يفتح شاشة الطلاب مفلترة على هذا المعلم |
| إجراءات | ✏️ تعديل ، 🗑️ حذف |

### ب. المصدران

| المصدر | كيف يُنشأ | هل يظهر في جدول الطلاب؟ |
| :--- | :--- | :---: |
| **تلقائي** | بمجرد أن يصبح مستوى طالب = `معلم` | ✅ نعم — يظل طالباً وله سجل كامل |
| **يدوي** | بزر "إضافة معلم" في هذه الشاشة | ❌ لا — سجله في ورقة `teachers` فقط |

نموذج الإضافة اليدوية يطلب: **الاسم**، **رقم الهاتف**، **ملاحظات (اختياري)** — لا مستوى ولا سنة ولا معهد، لأنه ليس طالباً.

### ج. قواعد المزامنة التلقائية

تُنفَّذ داخل Apps Script في نفس العملية المقفولة، وتُسجَّل في `side_effects_json` ليتم التراجع عنها معاً:

| الحدث | التصرف |
| :--- | :--- |
| إضافة/تعديل طالب ومستواه أصبح `معلم` | إنشاء سجل معلم `source = تلقائي` بنفس الاسم والهاتف و `student_id`. |
| تعديل اسم أو هاتف طالب مرتبط بمعلم تلقائي | تحديث سجل المعلم تلقائياً. |
| تغيير مستوى طالب **من** `معلم` لمستوى آخر | أرشفة سجل المعلم التلقائي — **إلا** إذا كان متابعاً لطلاب (انظر د). |
| حذف طالب مرتبط بمعلم تلقائي | نفس القاعدة السابقة. |
| تعديل اسم معلم | **إعادة تسمية متتالية:** تحديث `mentor` لكل طلابه في نفس العملية. |
| استيراد باسم معلم غير موجود | تحذير أصفر في المعاينة + زر "إضافة هذا المعلم". |

### د. حماية المعلم المرتبط بطلاب

> ❌ *"لا يمكن تنفيذ هذه العملية: **حسام عادل** متابع لـ **7 طلاب**. انقل الطلاب لمعلم آخر أولاً."*
> [عرض الطلاب السبعة] [نقلهم إلى معلم آخر]

زر النقل يفتح قائمة المعلمين، وعند الاختيار ينفّذ `bulkUpdate` ثم يعيد المحاولة تلقائياً. هذا يمنع أهم سيناريو فساد بيانات: طلاب معلّقون على اسم معلم غير موجود.

---

## 8. الشاشة الثالثة — مولّد الرسائل

### أ. محرك الاستعلام الموحد

**أهم قرار هيكلي في المشروع.** نسخة **واحدة** من حالة الاستعلام يشترك فيها الجدول ومولّد الرسائل:

```js
// مفتاح التخزين: "query_state_v1"
{
  search:  "",
  filters: [ { field: "level",  op: "in",     value: ["أ", "أ/"] },
             { field: "mentor", op: "equals", value: "حسام عادل" } ],
  sort:    [ { field: "mentor", dir: "asc" },
             { field: "level",  dir: "asc" } ],
  columns: [ { field: "name",   visible: true,  order: 0 },
             { field: "phone",  visible: true,  order: 1 },
             { field: "level",  visible: true,  order: 2 },
             { field: "mentor", visible: false, order: 3 } ]
}
```

- شاشة الطلاب تعرض الناتج كـ **جدول**، وشاشة الرسائل تعرض **نفس الناتج بالضبط** كـ **نص**.
- تغيير الفلتر في أي شاشة ينعكس على الأخرى فوراً — كود واحد، لا احتمال لاختلاف النتيجة.
- `columns` تؤثر على الجدول فقط؛ القالب هو من يحدد ما يظهر في الرسالة.

### ب. شريط المتغيرات

الضغط على أي رقاقة يُدرج المتغير في موضع المؤشر:

| الرقاقة | تُدرج |
| :--- | :--- |
| 👤 اسم الطالب | `{{الاسم}}` |
| 📱 رقم الهاتف | `{{الهاتف}}` |
| 🎓 المستوى | `{{المستوى}}` |
| 👨‍🏫 المعلم المتابع | `{{المعلم}}` |
| 🎒 الحالة الجامعية | `{{الحالة_الجامعية}}` |
| 🏛️ مسجل بالمعهد | `{{المعهد}}` |
| 📅 السنة بالمعهد | `{{السنة}}` |
| 📝 ملاحظات | `{{ملاحظات}}` |
| 📊 إجمالي الطلاب | `{{إجمالي_الطلاب}}` |
| 🔢 عدد طلاب المعلم | `{{عدد_طلاب_المعلم}}` |
| 📌 كتلة معلم | هيكل التجميع الكامل |

**هيكل التجميع:**
```text
{{#معلم}}
📍 {{اسم_المعلم}}
{{#طلاب}}
- {{الاسم}} ({{الهاتف}})
{{/طلاب}}

{{/معلم}}
```

المحرك يكتشف قسم `{{#معلم}}` فيجمّع الطلاب حسب `mentor` ويكرر ما بالداخل مرة لكل معلم، والحلقة `{{#طلاب}}` تكرر السطر لكل طالب تابع له. الطالب بلا معلم يُجمَّع تحت **"أخرى / غير محدد"**.

### ج. القالب الافتراضي

```text
{{#معلم}}
📍 المدرس: {{اسم_المعلم}}
{{#طلاب}}
- {{الاسم}} ({{الهاتف}})
{{/طلاب}}

{{/معلم}}
عدد الطلاب: {{إجمالي_الطلاب}}
```

**مثال على الناتج:**
```text
📍 المدرس: حسام عادل
- أحمد محمود (01012345678)
- سارة علي (01198765432)

📍 المدرس: مريم سمير
- منى أحمد (01533334444)

عدد الطلاب: 3
```

### د. الحفظ والنسخ

- القالب الحالي يُحفظ تلقائياً في `localStorage`، مع إمكانية حفظ أكثر من قالب **باسم** والتبديل بينها.
- زر **"استعادة القالب الافتراضي"**.
- صندوق نص كبير قابل للتحديد يدوياً، وأسفله زر عريض **"نسخ النص"** مع إشعار: *"تم نسخ النص بنجاح"*.

---

## 9. الشاشة الرابعة — سجل العمليات والتراجع

شبكة الأمان للتطبيق كله. تعرض كل ما حدث، وتتيح التراجع عن أي عملية.

### أ. الشكل

```text
+----------------------------------------------------+
|  سجل العمليات        [🗂️ الأرشيف]  [🔍 بحث]        |
+----------------------------------------------------+
| اليوم                                               |
|  ⏱️ 14:22  حذف جماعي — 12 طالب          [↩️ تراجع] |
|  ⏱️ 13:05  تعديل: أحمد محمود                        |
|            المستوى من (ب) إلى (أ)        [↩️ تراجع] |
|  ⏱️ 11:40  استيراد — 45 طالب من ملف إكسيل [↩️ تراجع] |
| أمس                                                 |
|  ⏱️ 18:10  إضافة معلم: مريم سمير          [↩️ تراجع] |
|  ⏱️ 17:55  حذف: سارة علي            ✅ تم التراجع   |
+----------------------------------------------------+
```

- مجمّعة بالتاريخ (اليوم / أمس / تواريخ سابقة)، الأحدث أولاً.
- كل سطر: الوقت + الوصف العربي الجاهز + زر التراجع.
- العملية التي تم التراجع عنها تظهر بعلامة ✅ وزرها معطّل.
- الضغط على أي سطر يفتح التفاصيل: القيم قبل وبعد، وأسماء كل السجلات المتأثرة.
- **فلترة وبحث** بنوع العملية أو باسم الطالب.

### ب. التراجع الفوري (Toast Undo)

بعد أي عملية مباشرة يظهر شريط سفلي لمدة **10 ثوانٍ**:
> *تم حذف 12 طالب.*  **↩️ تراجع**

هذا هو المسار السريع للأخطاء اللحظية. وشاشة السجل هي المسار المتأخر للأخطاء التي تُكتشف بعد ساعة أو يوم.

### ج. كيف يتم التراجع عن كل نوع

| العملية | آلية التراجع |
| :--- | :--- |
| **إضافة طالب/معلم** | أرشفة السجل المضاف |
| **تعديل** | إعادة كل الحقول لقيم `before_json` |
| **حذف** | استرجاع السجل من الأرشيف لمكانه الأصلي |
| **تعديل جماعي** | إعادة القيم السابقة لكل سجل على حدة |
| **حذف جماعي** | استرجاع كل السجلات من الأرشيف |
| **استيراد إكسيل** | أرشفة كل السجلات التي أنشأتها هذه العملية تحديداً |
| **الآثار الجانبية** | تُعكس معها: المعلم التلقائي يُحذف أو يُستعاد، وإعادة التسمية المتتالية تُرجَّع لكل الطلاب |

### د. متى يُرفض التراجع — القواعد الصريحة

التراجع ليس آمناً دائماً، وإخفاء ذلك أخطر من منعه. القواعد:

| الحالة | التصرف |
| :--- | :--- |
| تم التراجع عن العملية من قبل | **رفض كامل** — `410` |
| السجل عُدّل بعد هذه العملية (`updated_at` الحالي ≠ `after_json.updated_at`) | **رفض ذلك السجل تحديداً** مع رسالة: *"تم تعديل هذا الطالب بعد العملية — التراجع سيمسح التعديل الأحدث"*، مع خيار **"تراجع بالقوة"** بتأكيد صريح |
| استرجاع محذوف ورقم هاتفه صار مسجلاً لطالب آخر | **رفض ذلك السجل** — *"رقم الهاتف مسجل الآن لطالب آخر"* |
| استرجاع طالب ومعلمه المتابع لم يعد موجوداً | **قبول** مع وضعه تحت "أخرى / غير محدد" وتنبيه أصفر |
| التراجع عن عملية أقدم من 90 يوماً | **رفض** — السجل يُنظَّف تلقائياً بعد 90 يوماً |
| تراجع جماعي نجح جزئياً | **قبول جزئي** + تقرير واضح بما نُفِّذ وما استُثني ولماذا |

> **التراجع نفسه عملية:** يُسجَّل في السجل كسطر جديد نوعه `تراجع` يحمل `undo_of` للعملية الأصلية — فيمكنك التراجع عن التراجع (أي إعادة التنفيذ).

### هـ. شاشة الأرشيف

قائمة كل المحذوفين (طلاب ومعلمين) مع تاريخ الحذف، وبها بحث وفلترة، وزر **"استرجاع"** لكل سجل يعمل بنفس قواعد التراجع أعلاه. مفيدة عندما تريد استرجاع طالب واحد من عملية حذف جماعي قديمة بدل التراجع عنها كلها.

---

## 10. استيراد الإكسيل

```text
[اختيار ملف .xlsx / .csv]
          │
          ▼
[قراءة البيانات عبر SheetJS في المتصفح]
          │
          ▼
[فحص عناوين الأعمدة]
   ├── كل العناوين معروفة ──────────────► [تخطي شاشة المطابقة]
   └── عمود واحد أو أكثر غير معروف ────► [شاشة مطابقة الأعمدة]
                                          (تُملأ تلقائياً للمعروف منها)
          │
          ▼
[تطبيع البيانات والتحقق من القواعد]
          │
          ▼
[شاشة المعاينة: الصالح مقابل المرفوض مع سبب كل رفض]
          │
          ▼
[إرسال الصفوف الصالحة فقط عبر bulkCreate]
          │
          ▼
[سطر واحد في السجل يمكن التراجع عنه دفعة واحدة]
```

**العناوين المعروفة تلقائياً** (بعد التطبيع، وتقبل المرادفات): `الاسم` / `اسم الطالب` / `Name` — `الهاتف` / `رقم الهاتف` / `الموبايل` / `Phone` — `المستوى` / `Level` — `المعلم` / `المعلم المتابع` / `المدرس` — `الحالة الجامعية` / `طالب/خريج` — `المعهد` / `مسجل بالمعهد` — `السنة` / `السنة بالمعهد`.

### قواعد التحقق أثناء الاستيراد

| المشكلة | التصرف |
| :--- | :--- |
| رقم هاتف فقد صفر البداية | إصلاح تلقائي |
| رقم هاتف مخالف للنمط بعد الإصلاح | رفض الصف مع بيان السبب |
| مستوى غير موجود في القائمة | رفض الصف مع عرض القيمة الخاطئة |
| حالة جامعية غير `طالب` ولا `خريج` | رفض الصف |
| معلم غير موجود في `teachers` | قبول مع تحذير أصفر + زر "إضافة هذا المعلم" |
| `مسجل بالمعهد = نعم` والسنة فارغة | رفض الصف |
| سنة خارج القائمة الخمسة | رفض الصف |
| رقم مكرر داخل نفس الملف | قبول أول ظهور ورفض الباقي |
| رقم موجود بالفعل في قاعدة البيانات | رفض الصف مع عرض اسم الطالب الحالي |
| مستوى الصف = `معلم` | قبول + إنشاء سجل معلم تلقائي |

### معالجة رقم الهاتف

SheetJS غالباً يحوّل `01012345678` إلى `1012345678`، فيقوم `importer.js` بـ:
1. تحويل القيمة إلى نص صريح دائماً.
2. إزالة المسافات والرموز، وتحويل `+20` / `0020` / `20` في البداية إلى `0`.
3. لو الناتج 10 أرقام ويبدأ بـ `1` ← إضافة `0` في البداية.
4. التحقق النهائي مقابل `^01[0125][0-9]{8}$`.

---

## 11. بنية الملفات

```text
shabab-english/
├── index.html          # هيكل التطبيق (Single Page Application Shell)
├── app.css             # التصميم الكامل، RTL، عناصر الهواتف، Bottom Sheets
├── PLAN.md             # هذا الملف
├── apps-script/
│   └── Code.gs         # نسخة مرجعية من كود Apps Script (بدون الرقم السري)
└── js/
    ├── config.js       # رابط الـ Web App
    ├── constants.js    # المستويات والسنوات والحالة الجامعية
    ├── api.js          # الاتصال بـ Apps Script عبر fetch POST text/plain
    ├── auth.js         # شاشة الرقم السري وزر الخروج
    ├── store.js        # الحالة المركزية والتخزين المؤقت في localStorage
    ├── normalize.js    # تطبيع النصوص العربية وأرقام الهواتف
    ├── query.js        # حالة الاستعلام الموحدة
    ├── filter.js       # محرك التصفية
    ├── sort.js         # محرك الترتيب وترتيب المستويات المنطقي
    ├── columns.js      # إعدادات الأعمدة المرئية وترتيبها
    ├── table.js        # جدول الطلاب والتحديد والعمليات الجماعية
    ├── form.js         # نموذج التسجيل والتحقق
    ├── teachers.js     # شاشة المعلمين
    ├── importer.js     # تحليل ملفات Excel عبر SheetJS والتحقق
    ├── message.js      # واجهة مولّد الرسائل
    ├── template.js     # محرك دمج البيانات مع القوالب
    ├── history.js      # شاشة سجل العمليات والتراجع والأرشيف
    └── toast.js        # الإشعارات وشريط التراجع الفوري
```

> **SheetJS** تُحمَّل من CDN داخل `index.html` عند الحاجة فقط، وهي المكتبة الخارجية الوحيدة في المشروع.

---

## 12. خطة التنفيذ على مراحل

### المرحلة 0: الشيت والـ API الخلفي — **بما فيه السجل والأرشفة**
- **المخرج:** الشيت بأوراقه الخمس (`students`, `teachers`, `templates`, `archive`, `log`) + Apps Script منشور، وكل عملية كتابة تُسجّل في `log` وتؤرشف بدل الحذف.
- **معيار القبول:** نجاح طلبات POST بالرقم السري ورفضها بـ `401` بدونه؛ وكل عملية اختبارية تُنتج سطراً في `log`؛ وكل حذف يظهر في `archive`.

> ⚠️ **السجل والأرشفة يُبنيان من المرحلة صفر، لا يُضافان لاحقاً.** إضافتهما بعد بناء العمليات تعني إعادة كتابة كل دالة كتابة في السكريبت من جديد.

### المرحلة 1: هيكل التطبيق وطبقة البيانات
- **المخرج:** `index.html` + `app.css` + `store.js` + `api.js` + `auth.js` + `toast.js`.
- **معيار القبول:** فتح التطبيق على الهاتف، كتابة `55555`، تحميل البيانات وعرضها؛ وإعادة الفتح تعرض البيانات فوراً من الكاش ثم تحدّثها في الخلفية.

### المرحلة 2: التسجيل والتطبيع وجدول الطلاب
- **المخرج:** `form.js` + `normalize.js` + `table.js` — إضافة وتعديل وحذف طالب، كشف التكرار، الجدول بالعمود المثبت، زر واتساب.
- **معيار القبول:** إضافة طالب تنجح وتظهر في الجدول والشيت؛ تكرار رقم يُمنع مع عرض اسم الطالب الموجود؛ البحث عن "احمد" يجد "أحمد"؛ تسجيل `خريج` + `مسجل بالمعهد` + `تمهيدي` معاً ينجح بلا اعتراض.

### المرحلة 3: سجل العمليات والتراجع
- **المخرج:** `history.js` — شاشة السجل، شريط التراجع الفوري، شاشة الأرشيف، وكل قواعد رفض التراجع.
- **معيار القبول:** حذف طالب ثم التراجع يعيده كما كان؛ تعديل طالب ثم تعديله مرة أخرى ثم محاولة التراجع عن الأول تعطي التحذير الصحيح؛ التراجع عن التراجع يعمل.

### المرحلة 4: شاشة المعلمين والمزامنة التلقائية
- **المخرج:** `teachers.js` + منطق المزامنة + حماية المعلم المرتبط بطلاب.
- **معيار القبول:** تغيير مستوى طالب إلى `معلم` يضيفه للمعلمين فوراً؛ إضافة معلم يدوياً لا تضيفه للطلاب؛ حذف معلم متابع لطلاب يُمنع مع خيار نقل الطلاب؛ والتراجع عن هذه العمليات يعكس آثارها الجانبية.

### المرحلة 5: محرك الاستعلام الموحد وعناصر التحكم
- **المخرج:** `query.js` + `filter.js` + `sort.js` + `columns.js` مربوطة بالدرج السفلي.
- **معيار القبول:** الفلترة والترتيب يعملان على حقول مخفية؛ ترتيب المستوى يخرج (معلم ← ج/)؛ ترتيب الأعمدة يُحفظ بعد إغلاق المتصفح.

### المرحلة 6: التحديد والعمليات الجماعية
- **المخرج:** مربعات الاختيار + شريط الإجراءات السفلي + `bulkUpdate` و `bulkDelete`.
- **معيار القبول:** "تحديد الكل" يحدد نتيجة الفلترة فقط؛ نقل 10 طلاب لمعلم آخر ينجح في عملية واحدة قابلة للتراجع؛ الحذف الجماعي يطلب كتابة العدد.

### المرحلة 7: استيراد الإكسيل
- **المخرج:** `importer.js` + دمج SheetJS.
- **معيار القبول:** ملف فيه أرقام فقدت صفر البداية ومستوى خاطئ ورقم مكرر ← الأرقام تُصلَح والخاطئ يُعرض مع السبب ويُرفع الصالح فقط؛ والاستيراد كله قابل للتراجع بضغطة واحدة.

### المرحلة 8: مولّد الرسائل والقوالب
- **المخرج:** `message.js` + `template.js` + شريط الرقاقات + التجميع حسب المعلم.
- **معيار القبول:** الرسالة الناتجة تطابق تماماً ما هو معروض في الجدول تحت نفس الفلتر؛ النسخ بنقرة واحدة يعمل على الهاتف.

### المرحلة 9: النشر ودليل التشغيل
- **المخرج:** رفع الملفات على GitHub Pages + دليل تشغيل عربي.
- **معيار القبول:** فتح الرابط على الهاتف والعمل بالكامل بدون أخطاء في الـ Console، واختبار المسار من جهاز مشرف آخر.

---

## 13. الدخول والخصوصية

- عند فتح الموقع تظهر شاشة تطلب **رقماً سرياً واحداً** (الافتراضي `55555`).
- الرقم يُحفظ في `localStorage` فلا يُطلب مرة أخرى على نفس الهاتف حتى الضغط على **"خروج"**.
- نفس الرقم يُرسل مع كل طلب، والسكريبت يرفض أي طلب بدونه بـ `401`.
- تغيير الرقم = تعديل `PASSCODE` في Apps Script وإعادة النشر ← تخرج كل الأجهزة فوراً.

### ملاحظة صريحة عن حدود هذا الاختيار

بناءً على قرارك أن الحماية ليست مطلبًا في هذه المرحلة:

- لا يوجد مستخدمون منفصلون ولا صلاحيات مختلفة. **لكن سجل العمليات يوضّح ماذا حدث ومتى** — وإن لم يوضّح من فعله.
- رابط الـ Web App موجود في مستودع عام، فمن يعرفه ويجرّب `55555` يمكنه قراءة كل الأسماء والأرقام وتعديلها.
- **الترقية لاحقاً سهلة ولا تكسر شيئاً:** غيّر `PASSCODE` لقيمة طويلة عشوائية. لن يتغير سطر واحد في باقي التطبيق. أنصح بهذا فور دخول بيانات حقيقية، لكنه قرارك.
- ورقة Google Sheet تظل خاصة ولا تُنشر أبداً كـ CSV أو "أي شخص لديه الرابط".

---

## 14. خطوات التشغيل لصاحب المشروع

### 1. إعداد جدول Google Sheet و Apps Script
1. أنشئ جدول بيانات جديد على Google Drive وسمّه `Shabab English DB`.
2. من داخل الجدول: **Extensions (الإضافات)** ← **Apps Script**.
3. امسح الكود الموجود والصق محتوى ملف `apps-script/Code.gs` كاملاً.
4. تأكد أن قيمة `PASSCODE` في أعلى الملف هي `55555` (أو غيّرها لما تريد).
5. من القائمة المنسدلة أعلى المحرر اختر الدالة **`setup`** ثم اضغط **Run** — **مرة واحدة فقط**.
   ← ستنشئ الأوراق الخمس (`students`, `teachers`, `templates`, `archive`, `log`) بعناوينها كاملة، وتضبط أعمدة الهاتف على تنسيق نصي تلقائياً. سيطلب منك جوجل الموافقة على الصلاحيات في أول تشغيل.

> 🔁 **أعد تشغيل `setup` بعد أي تحديث لـ `Code.gs` يضيف عموداً أو ورقة** — الدالة آمنة ولا تمسح بيانات، تكتفي بكتابة صف العناوين. (مطلوبة تحديداً بعد تحديث `content` و `query_json` في ورقة `archive`.)

> ✅ **لا تكتب أي عنوان عمود بيدك، ولا توجد ورقة إعدادات.** المستويات والسنوات في الكود، وأسماء المعلمين تُدار من شاشة المعلمين داخل التطبيق.
> ⚠️ ورقتا `archive` و `log` يكتب فيهما البرنامج وحده — لا تعدّلهما يدوياً حتى لا تكسر التراجع. يفضّل إخفاؤهما (Hide sheet) بعد الإعداد.

### 2. نشر السكريبت كـ Web App
1. اضغط **Deploy** ← **New deployment** ← اختر النوع **Web app**.
2. الإعدادات: **Execute as:** Me — **Who has access:** Anyone.
3. اضغط **Deploy** ثم وافق على الصلاحيات.
4. انسخ **Web App URL**.
5. *(اختياري)* لتفعيل تنظيف السجل تلقائياً: **Triggers** ← **Add Trigger** ← الدالة `dailyMaintenance` ← **Day timer**.

> ⚠️ في كل مرة تعدّل الكود لاحقاً: **Deploy ← Manage deployments ← Edit ← New version**، وإلا لن يتغير شيء.

### 3. رفع الموقع على GitHub Pages
1. أنشئ مستودعاً جديداً باسم `shabab-english` واجعله **Public**.
2. افتح `js/config.js` والصق **Web App URL** بداخله.
3. ارفع ملفات المشروع.
4. **Settings** ← **Pages** ← **Branch:** `main` ← **Save**.
5. انتظر دقيقة وسيظهر الرابط.

### 4. التشغيل أول مرة على الهاتف
1. افتح الرابط من متصفح الهاتف.
2. اكتب `55555` واضغط دخول.
3. من قائمة المتصفح اختر **"إضافة إلى الشاشة الرئيسية"**.
4. **ابدأ بشاشة المعلمين** وسجّل المعلمين بنفسك، لأن كل طالب يحتاج معلماً متابعاً عند التسجيل.
5. لتجهيز مشرف جديد: أعطه الرابط والرقم السري فقط.

---

## 15. المخاطر وكيفية التعامل معها

| الأولوية | الخطر | التأثير | حل الوقاية والتعامل |
| :--- | :--- | :--- | :--- |
| **P0** | **تضخم ورقة `log` وبطء التطبيق** | كل عملية تكتب سطراً بحقول JSON كبيرة، فتبطؤ القراءة مع الوقت. | `list` تجلب آخر 200 عملية فقط وبدون حقول JSON؛ وتنظيف تلقائي لما تجاوز 90 يوماً. |
| **P0** | **تنسيق أرقام الهواتف** | فقدان صفر البداية في الإكسيل أو في الشيت. | تنسيق `phone` كـ Plain text في كل الأوراق + معالجة نصية صريحة في `importer.js`. |
| **P1** | **تراجع يمسح تعديلاً أحدث** | التراجع عن عملية قديمة يعيد قيماً بالية فوق تعديل جديد. | فحص `updated_at` قبل كل تراجع، ورفض السجل المتغيّر مع رسالة صريحة وخيار "تراجع بالقوة" بتأكيد منفصل. |
| **P1** | **الحذف الجماعي بالخطأ** | ضغطة واحدة تمسح عشرات السجلات. | الأرشفة بدل الحذف + تأكيد بكتابة العدد + شريط تراجع فوري + إمكانية التراجع لاحقاً من السجل. |
| **P1** | **طلاب معلّقون على معلم محذوف** | حذف معلم أو إنزال مستواه يترك طلابه بلا متابع. | منع العملية بـ `428` مع عرض العدد وخيار نقل الطلاب قبل التنفيذ. |
| **P2** | **تضارب تعديلات المشرفين** | مشرف يعدّل على بيانات قديمة فيمسح تعديل آخر. | `LockService.waitLock(10000)` + فحص `updated_at` ورفض المتضارب بـ `409`. |
| **P2** | **الوصول غير المصرّح به** | `55555` قابل للتخمين والرابط في مستودع عام. | قرار مقبول حالياً بموافقتك؛ الترقية = تغيير `PASSCODE` بلا أي تعديل آخر. |
| **P2** | **العبث اليدوي بورقتَي `archive` و `log`** | تعديل أو حذف صفوف يدوياً يكسر التراجع. | تحذير في الدليل + إخفاء الورقتين (Hide sheet) بعد الإعداد + تحقق دفاعي في السكريبت يتجاهل السطر التالف بدل الانهيار. |
| **P3** | **انقطاع الشبكة أثناء الحفظ** | عدم وصول البيانات وظن المشرف أنها حُفظت. | تنبيهات حالة صريحة (`جاري الحفظ...` / `تم الحفظ` / `فشل الاتصال`) مع الاحتفاظ ببيانات النموذج لإعادة المحاولة. |
| **P3** | **بطء استجابة Apps Script** | كل طلب يستغرق 1-3 ثوانٍ على بيانات الموبايل. | العرض الفوري من الكاش ثم التحديث في الخلفية، والكتابة المتفائلة مع التراجع عند الفشل. |

---

## 16. الفرضيات المعتمدة

كل النقاط السابقة حُسمت. الفرضيات المتبقية مذكورة هنا للعلم، ولا تعطّل بدء التنفيذ:

1. **مدة الاحتفاظ بالسجل: 90 يوماً.** بعدها لا يمكن التراجع عن العملية، لكن السجلات المحذوفة تبقى في الأرشيف للأبد.
2. **الأرشيف لا يُنظَّف تلقائياً** — يكبر ببطء ولا يؤثر على الأداء لأنه لا يُقرأ إلا عند فتح شاشة الأرشيف.
3. **قائمة المعلمين تُدخَل يدوياً بعد التسليم** من شاشة المعلمين، حسب طلبك.
4. **التعديل الجماعي على حقل واحد في المرة.** لو احتجت تغيير حقلين معاً على نفس المجموعة، تُنفَّذ عمليتان وتظهران كسطرين في السجل.
